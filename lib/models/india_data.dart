import 'package:flutter/foundation.dart';

class IndiaData {
  int total, recovered, deaths, active, tests, todayTests;

  IndiaData(
      {@required this.total,
      @required this.recovered,
      @required this.deaths,
      @required this.active,
      @required this.tests,
      @required this.todayTests});
}
